#include <stdio.h>

int main() {
    double price;
    double min = 99999.0;
    double max = 0.0;
    
    // TODO: Create a loop that reads 'price'.
    // Stop if price == -1.
    // Update min and max inside the loop.
    
    printf("Highest: %.1lf, Lowest: %.1lf\n", max, min);
    return 0;
}