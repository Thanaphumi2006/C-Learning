#include <stdio.h>

int main() {
    int maxRow = 3, maxCol = 3;
    double maxTemp = 0.0;
    int bestRow = 0, bestCol = 0;
    
    // TODO: Nested loops (row from 0-2, col from 0-2).
    // double temp = (row * 1.5) + (col * 2.1);
    // Track which row/col produced the highest temp.
    
    printf("Hottest at Row: %d, Col: %d\n", bestRow, bestCol);
    return 0;
}