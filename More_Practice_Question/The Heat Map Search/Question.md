Problem: Find the hottest coordinate on a grid.

Formula: Temp = (row * 1.5) + (col * 2.1)

Grid Size: maxRow = 5, maxCol = 5

Example:

Check (0,0), (0,1)... up to (4,4).

Output: "Hottest Point: Row 4, Col 4" (Since higher row/col gives higher temp).