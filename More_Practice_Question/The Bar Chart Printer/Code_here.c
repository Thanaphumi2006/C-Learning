#include <stdio.h>

void printBar(int length) {
    // TODO: Print '#' length times. Then print newline.
}

void printChart(int data[], int size) {
    // TODO: Loop through data and call printBar for each number.
}

int main() {
    int data[] = {1, 3};
    printChart(data, 2);
    return 0;
}