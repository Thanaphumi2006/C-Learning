#include <stdio.h>

void printPattern() {
    // TODO: Loop from 1 to 20.
    // If number is divisible by 5, print "X ".
    // Otherwise, print the number followed by a space (e.g., "1 ").
    
}

int main() {
    printPattern();
    printf("\n");
    return 0;
}