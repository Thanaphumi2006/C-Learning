#include <stdio.h>

void traffic(int duration, int interval) {
    // TODO: Loop from minute = 1 to duration.
    // If minute % interval == 0, print "minute: [GO]\n"
    // Else, print "minute: [STOP]\n"
    
}

int main() {
    int duration, interval;
    scanf("%d %d", &duration, &interval);
    traffic(duration, interval);
    return 0;
}