#include <stdio.h>

int main() {
    int limit = 100;
    int speed;
    
    // TODO: 1. Create a loop that scans for 'speed'.
    // If speed is < 0, print "Invalid" and loop again.
    
    // TODO: 2. Once valid, check conditions:
    // <= 100: Print "Safe"
    // 101-120: Print "$50 Fine"
    // > 120: Print "$150 Fine"
    
    return 0;
}