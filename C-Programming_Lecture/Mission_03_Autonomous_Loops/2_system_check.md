# 🔐 Security Clearance: Level 3

**Q1. Which loop is best when you know you need to run exactly 10 times?**
A) `while`
B) `for`
C) `if`

**Q2. Look at this code. What happens?**
```c
int x = 0;
while (x < 5) {
    printf("Hello\n");
}

A) It prints "Hello" 5 times. 
B) It prints "Hello" forever (Infinite Loop). 
C) It crashes immediately.

Q3. What does break do inside a loop? 
A) Breaks the computer. 
B) Pauses the loop for 1 second. 
C) Exits the loop immediately and stops repeating.















(Answers: 1-B, 2-B (Because x never increases!), 3-C)