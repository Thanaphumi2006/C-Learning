/*
 * MISSION TASK: LAUNCH COUNTDOWN
 * ------------------------------
 * TASK: Write a countdown timer for the Mars Return Vehicle.
 * LOGIC:
 * 1. User enters a starting number (e.g., 10).
 * 2. Count down to 1.
 * 3. At the end, print "LIFTOFF!".
 */

#include <stdio.h>

int main() {
    
    int t_minus;

    // 1. INPUT
    printf("Enter T-Minus seconds: ");
    scanf("%d", &t_minus);

    printf("\n--- INITIATING COUNTDOWN ---\n");

    // ==========================================
    // TODO: WRITE THE LOOP BELOW
    // ==========================================

    // TASK 1: Create a loop that runs WHILE 't_minus' is greater than 0.
    // Inside the loop:
    //    a. Print "T-Minus [number]..."
    //    b. Decrease 't_minus' by 1. (Don't forget this!)
    
    // Hint: Use a 'while' loop.
    


    // TASK 2: Print "LIFTOFF!!" after the loop finishes.
    


    return 0;
}