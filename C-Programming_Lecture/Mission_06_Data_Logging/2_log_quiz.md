# 🔐 Security Clearance: Level 6

**Q1. Which mode wipes the file and starts fresh?**
A) `"r"` (Read)
B) `"w"` (Write)
C) `"a"` (Append)

**Q2. What happens if you forget `fclose(file)`?**
A) Nothing bad.
B) The file might remain "locked" or empty because the data wasn't saved.
C) The computer explodes.

**Q3. Which function puts text INTO a file?**
A) `scanf`
B) `fgets`
C) `fprintf`






















*(Answers: 1-B, 2-B, 3-C)*
