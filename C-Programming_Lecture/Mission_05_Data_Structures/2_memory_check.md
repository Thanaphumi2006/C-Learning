# 🔐 Security Clearance: Level 5

**Q1. If I create `int data[5];`, what is the index of the LAST item?**
A) 5
B) 4
C) 0

**Q2. What happens if you try to access `data[10]` in an array of size 5?**
A) It loops back to the start.
B) It creates a new slot automatically.
C) It accesses garbage memory (dangerous) or crashes.

**Q3. How do you print the SECOND item in `int numbers[] = {10, 20, 30};`?**
A) `printf("%d", numbers[1]);`
B) `printf("%d", numbers[2]);`
C) `printf("%d", numbers(2));`






















*(Answers: 1-B, 2-C, 3-A)*