# 🔐 Security Clearance: Level 8

**Q1. What does `strcmp(a, b)` return if the strings are identical?**
A) 1 (True)
B) 0 (Zero Difference)
C) -1

**Q2. Which library must you include to use `strcmp` or `strlen`?**
A) `<math.h>`
B) `<stdlib.h>`
C) `<string.h>`

**Q3. What happens if you use `strcat(small_array, huge_string)`?**
A) It works perfectly.
B) It resizes the array automatically.
C) Buffer Overflow (Crash/Security Risk) because the destination is too small.






























*(Answers: 1-B, 2-C, 3-C)*