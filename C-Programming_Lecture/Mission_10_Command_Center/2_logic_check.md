# 🔐 Security Clearance: Level 10 (Commander)

**Q1. What happens if you forget `break;` inside a switch case?**
A) The code crashes.
B) It stops automatically.
C) It "falls through" and runs the next case too (Bug!).

**Q2. What is the job of the `default` case?**
A) To handle any input that didn't match the other cases.
B) To restart the computer.
C) To define variables.

**Q3. How do we stop a `while (running == 1)` loop?**
A) Delete the code.
B) Change the variable `running` to 0.
C) Unplug the computer.
































*(Answers: 1-C, 2-A, 3-B)*