# 🔐 Security Clearance: Level 9

**Q1. What symbol do you use to get the ADDRESS of a variable?**
A) `*` (Star)
B) `&` (Ampersand)
C) `%` (Percent)

**Q2. If `ptr` points to `score`, what does `*ptr = 0` do?**
A) It makes the address 0 (Null Pointer).
B) It changes the value of `score` to 0.
C) It deletes the variable.

**Q3. How do you declare a pointer that holds an integer's address?**
A) `int &ptr;`
B) `pointer int ptr;`
C) `int *ptr;`





















*(Answers: 1-B, 2-B, 3-C)*