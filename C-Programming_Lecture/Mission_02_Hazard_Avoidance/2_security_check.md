# 🔐 Security Clearance: Level 2

**Q1. Which symbol is used to ASK if two numbers are equal?**
A) `=`
B) `==`
C) `||`

**Q2. Look at this code:**
```c
int fuel = 5;
if (fuel > 10 && fuel < 50) ...

Will this code run? 
A) Yes. 
B) No, because 5 is not greater than 10. 
C) Yes, because it is less than 50.

# Q3. What does || mean? 
A) AND (Both must be true) 
B) OR (At least one must be true) 
C) NOT (Reverse the logic)













(Answers: 1-B, 2-B, 3-B)