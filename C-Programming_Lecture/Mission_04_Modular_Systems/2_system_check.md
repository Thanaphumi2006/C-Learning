# 🔐 Security Clearance: Level 4 - Protocols

**Q1. A function is defined as `void launch_rocket()`. What does `void` mean?**
A) The function needs a void input.
B) The function returns no value to the main program.
C) The function is invalid.

**Q2. Look at this code:**
```c
void set_speed() {
    int speed = 100;
}

int main() {
    printf("%d", speed);
}
Will this compile? 
A) Yes, speed is 100. 
B) No, speed is a local variable inside set_speed and cannot be seen by main. 
C) Yes, but it prints 0.

Q3. If you want to send a number INTO a function, where do you put it? 
A) In the return statement.
B) Inside the parentheses () as a parameter. 
C) You cannot send numbers into functions.











(Answers: 1-B, 2-B, 3-B)